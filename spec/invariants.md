# Invariants

```text
PROPOSED != AUTHORIZED
AUTHORIZED != EXECUTED
OBSERVED != INFERRED
INFERRED != CANONICAL
VISIBLE != WRITABLE
WRITABLE != AUTHORITATIVE
```

```text
DOGGO(I) cannot produce:
  - CanonicalMutation
  - Execution
  - Authorization
  - Promotion
```
